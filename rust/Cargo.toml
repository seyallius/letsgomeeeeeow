[package]
name = "letsgomeeeeeow"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = "fat"             # better linking optimization - better inlining across std and our crate (tf?)
panic = "abort"
debug = true

[dependencies]
libc = "0.2.180"
tempfile = "3.24.0"
